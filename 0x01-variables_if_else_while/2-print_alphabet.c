#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: alphabbets in lower case
 *
 * Return: always 0 (Success)
 *
 */

int main(void)
{
	char dee;

	for (dee = 'a'; dee <= 'z'; dee++)
	{
		putchar(dee);
	}
	putchar('\n');
	return (0);
}
